;; extension todo
(setq org-todo-keywords
      '((sequence "TODO(t)" "NEXT(n)" "WAIT(w)" "|" "DONE(d)" "SOMEDAY(s)")))

;; display code color
(setq org-src-fontify-natively t)

;; hide leading stars
(setq org-hide-leading-stars t)
